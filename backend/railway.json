{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd .. && uvicorn backend.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health"
  }
}
